import SignUnFormSection from "@/components/page-components/patient/auth/SignUpPageSection";
import { Metadata } from "next";

export const metadata: Metadata = {
   title: "SignUp",
};

const Register = () => {
   return <SignUnFormSection />;
};

export default Register;
