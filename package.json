{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run dev --prefix client\" \"npm run serve --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}