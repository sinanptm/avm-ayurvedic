# MongoDB connection URL
MONGODB_URL=mongodb://localhost:27017/AVM

# Server Port
PORT=8000

# Environment (development, production)
NODE_ENV=dev

# Client URL for CORS
CLIENT_URL=http://localhost:3000

# Nodemailer Email Config (replace with test credentials)
SENDER_EMAIL=test@example.com
NODEMAILER_PASSKEY=test-nodemailer-passkey

# JWT Secrets (use test values for development)
ACCESS_TOKEN_SECRET=test-access-token-secret
REFRESH_TOKEN_SECRET=test-refresh-token-secret

# AWS Credentials (use mock/test credentials if needed)
AWS_REGION=eu-north-1
AWS_ACCESS_KEY_ID=test-aws-access-key-id
AWS_SECRET_ACCESS_KEY=test-aws-secret-access-key

# AWS S3 bucket name (use a test bucket)
S3_BUCKET_NAME=test-avm-ayurvedic-bucket

# Stripe API keys (replace with Stripe test keys)
STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXXXXXX

# Google Gemini API Key (replace with test or mock key)
GEMINI_API_KEY=test-gemini-api-key
